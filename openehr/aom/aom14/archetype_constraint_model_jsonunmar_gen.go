// Code generated by bmmgen; DO NOT EDIT.
// Source: openehr_am_1.4.0.bmm.json + openehr_base_1.3.0.bmm.json

package aom14

import (
	"encoding/json"
	"fmt"

	"github.com/cadasto/openehr-sdk-go/openehr/rm"
	"github.com/cadasto/openehr-sdk-go/openehr/rm/typereg"
)

// BMM package: org.openehr.am.aom14.archetype.constraint_model — canonical-JSON UnmarshalJSON companions

type ArchetypeInternalRefJSONUnmarshaller struct {
	Class string `json:"_type"`
	// RMTypeName Reference model type that this node corresponds to.
	RMTypeName string `json:"rm_type_name"`
	// Occurrences Occurrences of this object node in the data, under the owning attribute. Upper limit can only be greater than 1 if owning attribute has a cardinality of more than 1).
	Occurrences rm.Interval[Integer] `json:"occurrences"`
	// NodeID Semantic identifier of this node, used to distinguish sibling nodes. All nodes must have a node_id; for nodes under a container C_ATTRIBUTE, the id must be an id-code must be defined in the archetype terminology. For valid structures, all node ids are at-codes.
	// For C_PRIMITIVE_OBJECTs, it will have the special value Primitive_node_id.
	NodeID string `json:"node_id"`
	// TargetPath Reference to an object node using archetype path notation.
	TargetPath string `json:"target_path"`
}

// UnmarshalJSON decodes canonical openEHR JSON into ArchetypeInternalRef.
// Polymorphic fields are routed through typereg.DecodeAs so the
// concrete type is selected by `_type` at each polymorphic site.
// Missing/unknown/type-mismatch dispatch failures wrap typereg
// sentinels inside *typereg.DecodeError for errors.Is / errors.As.
func (a *ArchetypeInternalRef) UnmarshalJSON(data []byte) error {
	var aux ArchetypeInternalRefJSONUnmarshaller
	if err := json.Unmarshal(data, &aux); err != nil {
		return fmt.Errorf("canjson: ARCHETYPE_INTERNAL_REF: %w", err)
	}
	if aux.Class != "" && aux.Class != "ARCHETYPE_INTERNAL_REF" {
		return &typereg.DecodeError{
			Path:  "/_type",
			Inner: fmt.Errorf("canjson: expected %q, got %q: %w", "ARCHETYPE_INTERNAL_REF", aux.Class, typereg.ErrTypeMismatch),
		}
	}
	a.RMTypeName = aux.RMTypeName
	a.Occurrences = aux.Occurrences
	a.NodeID = aux.NodeID
	a.TargetPath = aux.TargetPath
	return nil
}

type ArchetypeSlotJSONUnmarshaller struct {
	Class string `json:"_type"`
	// RMTypeName Reference model type that this node corresponds to.
	RMTypeName string `json:"rm_type_name"`
	// Occurrences Occurrences of this object node in the data, under the owning attribute. Upper limit can only be greater than 1 if owning attribute has a cardinality of more than 1).
	Occurrences rm.Interval[Integer] `json:"occurrences"`
	// NodeID Semantic identifier of this node, used to distinguish sibling nodes. All nodes must have a node_id; for nodes under a container C_ATTRIBUTE, the id must be an id-code must be defined in the archetype terminology. For valid structures, all node ids are at-codes.
	// For C_PRIMITIVE_OBJECTs, it will have the special value Primitive_node_id.
	NodeID string `json:"node_id"`
	// Includes List of constraints defining other archetypes that could be included at this point.
	Includes []Assertion `json:"includes,omitempty"`
	// Excludes List of constraints defining other archetypes that cannot be included at this point.
	Excludes []Assertion `json:"excludes,omitempty"`
}

// UnmarshalJSON decodes canonical openEHR JSON into ArchetypeSlot.
// Polymorphic fields are routed through typereg.DecodeAs so the
// concrete type is selected by `_type` at each polymorphic site.
// Missing/unknown/type-mismatch dispatch failures wrap typereg
// sentinels inside *typereg.DecodeError for errors.Is / errors.As.
func (a *ArchetypeSlot) UnmarshalJSON(data []byte) error {
	var aux ArchetypeSlotJSONUnmarshaller
	if err := json.Unmarshal(data, &aux); err != nil {
		return fmt.Errorf("canjson: ARCHETYPE_SLOT: %w", err)
	}
	if aux.Class != "" && aux.Class != "ARCHETYPE_SLOT" {
		return &typereg.DecodeError{
			Path:  "/_type",
			Inner: fmt.Errorf("canjson: expected %q, got %q: %w", "ARCHETYPE_SLOT", aux.Class, typereg.ErrTypeMismatch),
		}
	}
	a.RMTypeName = aux.RMTypeName
	a.Occurrences = aux.Occurrences
	a.NodeID = aux.NodeID
	a.Includes = aux.Includes
	a.Excludes = aux.Excludes
	return nil
}

type CComplexObjectJSONUnmarshaller struct {
	Class string `json:"_type"`
	// AssumedValue Value to be assumed if none sent in data.
	AssumedValue *any `json:"assumed_value,omitempty"`
	// RMTypeName Reference model type that this node corresponds to.
	RMTypeName string `json:"rm_type_name"`
	// Occurrences Occurrences of this object node in the data, under the owning attribute. Upper limit can only be greater than 1 if owning attribute has a cardinality of more than 1).
	Occurrences rm.Interval[Integer] `json:"occurrences"`
	// NodeID Semantic identifier of this node, used to distinguish sibling nodes. All nodes must have a node_id; for nodes under a container C_ATTRIBUTE, the id must be an id-code must be defined in the archetype terminology. For valid structures, all node ids are at-codes.
	// For C_PRIMITIVE_OBJECTs, it will have the special value Primitive_node_id.
	NodeID     string            `json:"node_id"`
	Attributes []json.RawMessage `json:"attributes,omitempty"` // polymorphic []CAttribute
}

// UnmarshalJSON decodes canonical openEHR JSON into CComplexObject.
// Polymorphic fields are routed through typereg.DecodeAs so the
// concrete type is selected by `_type` at each polymorphic site.
// Missing/unknown/type-mismatch dispatch failures wrap typereg
// sentinels inside *typereg.DecodeError for errors.Is / errors.As.
func (c *CComplexObject) UnmarshalJSON(data []byte) error {
	var aux CComplexObjectJSONUnmarshaller
	if err := json.Unmarshal(data, &aux); err != nil {
		return fmt.Errorf("canjson: C_COMPLEX_OBJECT: %w", err)
	}
	if aux.Class != "" && aux.Class != "C_COMPLEX_OBJECT" {
		return &typereg.DecodeError{
			Path:  "/_type",
			Inner: fmt.Errorf("canjson: expected %q, got %q: %w", "C_COMPLEX_OBJECT", aux.Class, typereg.ErrTypeMismatch),
		}
	}
	c.AssumedValue = aux.AssumedValue
	c.RMTypeName = aux.RMTypeName
	c.Occurrences = aux.Occurrences
	c.NodeID = aux.NodeID
	if aux.Attributes != nil {
		c.Attributes = make([]CAttribute, len(aux.Attributes))
		for idx, raw := range aux.Attributes {
			if len(raw) == 0 || string(raw) == "null" {
				continue
			}
			dv, err := typereg.DecodeAs[CAttribute](raw)
			if err != nil {
				return &typereg.DecodeError{Path: fmt.Sprintf("/attributes/%d", idx), Inner: err}
			}
			c.Attributes[idx] = dv
		}
	}
	return nil
}

type CMultipleAttributeJSONUnmarshaller struct {
	Class string `json:"_type"`
	// RMAttributeName Reference model attribute within the enclosing type represented by a C_OBJECT.
	RMAttributeName string `json:"rm_attribute_name"`
	// Existence Constraint on every attribute, regardless of whether it is singular or of a container type, which indicates whether its target object exists or not (i.e. is mandatory or not).
	Existence rm.Interval[Integer] `json:"existence"`
	Children  []json.RawMessage    `json:"children,omitempty"` // polymorphic []CObject
	// Cardinality Cardinality of this attribute constraint, if it constraints a container attribute.
	Cardinality Cardinality `json:"cardinality"`
}

// UnmarshalJSON decodes canonical openEHR JSON into CMultipleAttribute.
// Polymorphic fields are routed through typereg.DecodeAs so the
// concrete type is selected by `_type` at each polymorphic site.
// Missing/unknown/type-mismatch dispatch failures wrap typereg
// sentinels inside *typereg.DecodeError for errors.Is / errors.As.
func (c *CMultipleAttribute) UnmarshalJSON(data []byte) error {
	var aux CMultipleAttributeJSONUnmarshaller
	if err := json.Unmarshal(data, &aux); err != nil {
		return fmt.Errorf("canjson: C_MULTIPLE_ATTRIBUTE: %w", err)
	}
	if aux.Class != "" && aux.Class != "C_MULTIPLE_ATTRIBUTE" {
		return &typereg.DecodeError{
			Path:  "/_type",
			Inner: fmt.Errorf("canjson: expected %q, got %q: %w", "C_MULTIPLE_ATTRIBUTE", aux.Class, typereg.ErrTypeMismatch),
		}
	}
	c.RMAttributeName = aux.RMAttributeName
	c.Existence = aux.Existence
	if aux.Children != nil {
		c.Children = make([]CObject, len(aux.Children))
		for idx, raw := range aux.Children {
			if len(raw) == 0 || string(raw) == "null" {
				continue
			}
			dv, err := typereg.DecodeAs[CObject](raw)
			if err != nil {
				return &typereg.DecodeError{Path: fmt.Sprintf("/children/%d", idx), Inner: err}
			}
			c.Children[idx] = dv
		}
	}
	c.Cardinality = aux.Cardinality
	return nil
}

type CPrimitiveObjectJSONUnmarshaller struct {
	Class string `json:"_type"`
	// AssumedValue Value to be assumed if none sent in data.
	AssumedValue *any `json:"assumed_value,omitempty"`
	// RMTypeName Reference model type that this node corresponds to.
	RMTypeName string `json:"rm_type_name"`
	// Occurrences Occurrences of this object node in the data, under the owning attribute. Upper limit can only be greater than 1 if owning attribute has a cardinality of more than 1).
	Occurrences rm.Interval[Integer] `json:"occurrences"`
	// NodeID Semantic identifier of this node, used to distinguish sibling nodes. All nodes must have a node_id; for nodes under a container C_ATTRIBUTE, the id must be an id-code must be defined in the archetype terminology. For valid structures, all node ids are at-codes.
	// For C_PRIMITIVE_OBJECTs, it will have the special value Primitive_node_id.
	NodeID string          `json:"node_id"`
	Item   json.RawMessage `json:"item"` // polymorphic CPrimitive
}

// UnmarshalJSON decodes canonical openEHR JSON into CPrimitiveObject.
// Polymorphic fields are routed through typereg.DecodeAs so the
// concrete type is selected by `_type` at each polymorphic site.
// Missing/unknown/type-mismatch dispatch failures wrap typereg
// sentinels inside *typereg.DecodeError for errors.Is / errors.As.
func (c *CPrimitiveObject) UnmarshalJSON(data []byte) error {
	var aux CPrimitiveObjectJSONUnmarshaller
	if err := json.Unmarshal(data, &aux); err != nil {
		return fmt.Errorf("canjson: C_PRIMITIVE_OBJECT: %w", err)
	}
	if aux.Class != "" && aux.Class != "C_PRIMITIVE_OBJECT" {
		return &typereg.DecodeError{
			Path:  "/_type",
			Inner: fmt.Errorf("canjson: expected %q, got %q: %w", "C_PRIMITIVE_OBJECT", aux.Class, typereg.ErrTypeMismatch),
		}
	}
	c.AssumedValue = aux.AssumedValue
	c.RMTypeName = aux.RMTypeName
	c.Occurrences = aux.Occurrences
	c.NodeID = aux.NodeID
	if len(aux.Item) > 0 && string(aux.Item) != "null" {
		dv, err := typereg.DecodeAs[CPrimitive](aux.Item)
		if err != nil {
			return &typereg.DecodeError{Path: "/item", Inner: err}
		}
		c.Item = dv
	}
	return nil
}

type CSingleAttributeJSONUnmarshaller struct {
	Class string `json:"_type"`
	// RMAttributeName Reference model attribute within the enclosing type represented by a C_OBJECT.
	RMAttributeName string `json:"rm_attribute_name"`
	// Existence Constraint on every attribute, regardless of whether it is singular or of a container type, which indicates whether its target object exists or not (i.e. is mandatory or not).
	Existence rm.Interval[Integer] `json:"existence"`
	Children  []json.RawMessage    `json:"children,omitempty"` // polymorphic []CObject
}

// UnmarshalJSON decodes canonical openEHR JSON into CSingleAttribute.
// Polymorphic fields are routed through typereg.DecodeAs so the
// concrete type is selected by `_type` at each polymorphic site.
// Missing/unknown/type-mismatch dispatch failures wrap typereg
// sentinels inside *typereg.DecodeError for errors.Is / errors.As.
func (c *CSingleAttribute) UnmarshalJSON(data []byte) error {
	var aux CSingleAttributeJSONUnmarshaller
	if err := json.Unmarshal(data, &aux); err != nil {
		return fmt.Errorf("canjson: C_SINGLE_ATTRIBUTE: %w", err)
	}
	if aux.Class != "" && aux.Class != "C_SINGLE_ATTRIBUTE" {
		return &typereg.DecodeError{
			Path:  "/_type",
			Inner: fmt.Errorf("canjson: expected %q, got %q: %w", "C_SINGLE_ATTRIBUTE", aux.Class, typereg.ErrTypeMismatch),
		}
	}
	c.RMAttributeName = aux.RMAttributeName
	c.Existence = aux.Existence
	if aux.Children != nil {
		c.Children = make([]CObject, len(aux.Children))
		for idx, raw := range aux.Children {
			if len(raw) == 0 || string(raw) == "null" {
				continue
			}
			dv, err := typereg.DecodeAs[CObject](raw)
			if err != nil {
				return &typereg.DecodeError{Path: fmt.Sprintf("/children/%d", idx), Inner: err}
			}
			c.Children[idx] = dv
		}
	}
	return nil
}

type CardinalityJSONUnmarshaller struct {
	Class string `json:"_type"`
	// Interval The interval of this cardinality.
	Interval rm.Interval[Integer] `json:"interval"`
	// IsOrdered True if the members of the container attribute to which this cardinality refers are ordered.
	IsOrdered bool `json:"is_ordered"`
	// IsUnique True if the members of the container attribute to which this cardinality refers are unique.
	IsUnique bool `json:"is_unique"`
}

// UnmarshalJSON decodes canonical openEHR JSON into Cardinality.
// Polymorphic fields are routed through typereg.DecodeAs so the
// concrete type is selected by `_type` at each polymorphic site.
// Missing/unknown/type-mismatch dispatch failures wrap typereg
// sentinels inside *typereg.DecodeError for errors.Is / errors.As.
func (c *Cardinality) UnmarshalJSON(data []byte) error {
	var aux CardinalityJSONUnmarshaller
	if err := json.Unmarshal(data, &aux); err != nil {
		return fmt.Errorf("canjson: CARDINALITY: %w", err)
	}
	if aux.Class != "" && aux.Class != "CARDINALITY" {
		return &typereg.DecodeError{
			Path:  "/_type",
			Inner: fmt.Errorf("canjson: expected %q, got %q: %w", "CARDINALITY", aux.Class, typereg.ErrTypeMismatch),
		}
	}
	c.Interval = aux.Interval
	c.IsOrdered = aux.IsOrdered
	c.IsUnique = aux.IsUnique
	return nil
}

type ConstraintRefJSONUnmarshaller struct {
	Class string `json:"_type"`
	// RMTypeName Reference model type that this node corresponds to.
	RMTypeName string `json:"rm_type_name"`
	// Occurrences Occurrences of this object node in the data, under the owning attribute. Upper limit can only be greater than 1 if owning attribute has a cardinality of more than 1).
	Occurrences rm.Interval[Integer] `json:"occurrences"`
	// NodeID Semantic identifier of this node, used to distinguish sibling nodes. All nodes must have a node_id; for nodes under a container C_ATTRIBUTE, the id must be an id-code must be defined in the archetype terminology. For valid structures, all node ids are at-codes.
	// For C_PRIMITIVE_OBJECTs, it will have the special value Primitive_node_id.
	NodeID string `json:"node_id"`
	// Reference Reference to a constraint in the archetype local ontology.
	Reference string `json:"reference"`
}

// UnmarshalJSON decodes canonical openEHR JSON into ConstraintRef.
// Polymorphic fields are routed through typereg.DecodeAs so the
// concrete type is selected by `_type` at each polymorphic site.
// Missing/unknown/type-mismatch dispatch failures wrap typereg
// sentinels inside *typereg.DecodeError for errors.Is / errors.As.
func (c *ConstraintRef) UnmarshalJSON(data []byte) error {
	var aux ConstraintRefJSONUnmarshaller
	if err := json.Unmarshal(data, &aux); err != nil {
		return fmt.Errorf("canjson: CONSTRAINT_REF: %w", err)
	}
	if aux.Class != "" && aux.Class != "CONSTRAINT_REF" {
		return &typereg.DecodeError{
			Path:  "/_type",
			Inner: fmt.Errorf("canjson: expected %q, got %q: %w", "CONSTRAINT_REF", aux.Class, typereg.ErrTypeMismatch),
		}
	}
	c.RMTypeName = aux.RMTypeName
	c.Occurrences = aux.Occurrences
	c.NodeID = aux.NodeID
	c.Reference = aux.Reference
	return nil
}
